version: '3.8'

services:
  dlnhks-game-server:
    image: trantu30950/postgresql-sever:latest
    build: .
    container_name: postgresql-sever
    restart: always
    ports:
      - '5000:5000'
    volumes:
      - ./.env:/app/.env